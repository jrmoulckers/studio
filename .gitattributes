# studio:base:start
# synced from jrmoulckers/.github — canonical source; do not edit here
# Normalize every detected text file to LF in the index and working tree.
# Git's automatic text detection leaves binary files byte-for-byte unchanged.
* text=auto eol=lf
# studio:base:end

# Studio-local rules follow the managed region. A later matching line wins in
# .gitattributes and the canonical stanza's `*` matches everything below, so a
# local rule placed above it would be downgraded from `text: set` to
# `text: auto`. Since jrmoulckers/.github#125 the sync engine prepends the
# managed region for this file kind rather than appending it, so this ordering
# is guaranteed by the transport and no longer depends on how this file was
# hand-seeded. Keep local rules below the region regardless.

# The committed @jrm/tokens distribution is copied verbatim into member repos by
# the registry-free sync engine, so its bytes must be deterministic (LF) on every
# platform regardless of core.autocrlf. Keep the whole dist tree LF.
packages/tokens/dist/** text eol=lf
